type Post {
  id: ID
  title: String
  createdAt: String
  modifiedAt: String
}

type BlogPost {
  title: String
  createdAt: String
  id: ID
  test: String
}

type MailPost {
  title: String
  createdAt: String
  test: String
}

type EmailPost {
  title: String
  createdAt: String
  modifiedAt: String
}

type Comment {
  id: ID
}

type Query {
  post: Post!
}
